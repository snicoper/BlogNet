@model Web.ViewModels.ArticleViewModels.EmailRecommendViewModel
Desde @Model.SiteName un usuario con el email @Model.From ha considerado que te puede interesar un articulo.

@Model.Title

Para ver el articulo visita el siguiente enlace:

@Model.CallBack

Gracias desde @Model.SiteName
